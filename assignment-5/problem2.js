//user data which is already on data base
let data_username='noob';
let data_password='demon_slayer';
//user data that i know
let user_name='noob';
let password='demon_slayer';
if (data_username==user_name) {
  if (data_password==password) {
    console.log('user can login');
  }
}
else {
  console.log('invalid username or password');
  
}